import { BasicAttributeDirective } from './basic-attribute.directive';

describe('BasicAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
