import { AdvanceAttributeDirective } from './advance-attribute.directive';

describe('AdvanceAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
